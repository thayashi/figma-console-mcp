# Agent Templates

Project-level workflow templates now live under [`project_templates/`](/home/toshi/dev/figma-console-mcp/project_templates/).

Use `project_templates/base/` as the default starting point, then merge one or more presets as needed:

- `app-mockup/`
- `design-system/`
- `code-connect/`

The files in this directory are kept only as lightweight compatibility notes so older links continue to resolve.
